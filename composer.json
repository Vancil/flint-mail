{
    "name": "vancil/flint-mail",
    "description": "Full-featured mail package for Flint — Mailables, queueing, and HTTP API drivers",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "repositories": [
        {"type": "vcs", "url": "https://github.com/Vancil/flint"}
    ],
    "require": {
        "php": ">=8.1",
        "vancil/flint": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "Vancil\\FlintMail\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit"
    },
    "config": {
        "optimize-autoloader": true
    }
}
